package io.werk24.model;

public enum W24TechreadMessageType {
    /**
     * Message Type of the message that is sent
     * from the server to the client in response to
     * a request.
     */
    ASK,
    PROGRESS,
    ERROR;
}
